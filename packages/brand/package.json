{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/brand",
  "version": "1.18.30",
  "description": "Env-driven brand identity (name, tagline, block wordmark) shared across the opencode surfaces",
  "private": true,
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./env": "./src/env.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}